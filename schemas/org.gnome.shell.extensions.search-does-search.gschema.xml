<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.search-does-search"
          path="/org/gnome/shell/extensions/search-does-search/">

    <!-- Four keys, all enums. The page is rendered by panel/sds-renderer.js (a
         separate process — GNOME Shell cannot host WebKit) and shown in the
         overview by the page view; these keys are read by the page view, the
         section placement and the prefs UI. -->

    <key name="engine" type="s">
      <choices>
        <choice value="duckduckgo"/>
        <choice value="google"/>
        <choice value="bing"/>
        <choice value="brave"/>
        <choice value="startpage"/>
      </choices>
      <default>'duckduckgo'</default>
      <summary>Results page engine</summary>
      <description>
        The search engine shown in Activities. Uses the normal search page.
        Availability depends on the engine and network; verification challenges
        can be continued in your default browser. Engines never switch automatically.
      </description>
    </key>

    <key name="link-mode" type="s">
      <choices>
        <choice value="contained"/>
        <choice value="browser"/>
      </choices>
      <default>'contained'</default>
      <summary>What a clicked link does</summary>
      <description>
        'contained' follows the link inside the overview's page and shows a back
        control carrying how many pages you have followed; middle-click or
        Ctrl+click still opens a link in the browser. 'browser' hands every link to
        the default browser and closes the overview. Enter on the section always
        opens what you are looking at in the browser.

        Contained is not a browser: no forward, no address bar, no tabs. Anything
        the view cannot display — a download, a PDF — goes to the real browser
        whichever mode this is set to.
      </description>
    </key>

    <key name="section-visibility" type="s">
      <choices>
        <choice value="always"/>
        <choice value="no-other-results"/>
      </choices>
      <default>'always'</default>
      <summary>When the web results section is shown</summary>
      <description>
        'always' shows the section for every search. 'no-other-results' shows it
        only when no application, file or other provider matched; your browser's own
        "Search online" entry answers every query, so it does not count as a match.
        The page is rendered either way, so showing it is instant.
      </description>
    </key>

    <key name="section-placement" type="s">
      <choices>
        <choice value="top"/>
        <choice value="top-when-alone"/>
        <choice value="default"/>
      </choices>
      <default>'top-when-alone'</default>
      <summary>Where the section sits among the search results</summary>
      <description>
        'top' pins the section above every other one. 'top-when-alone' moves it up
        only when nothing else matched (the browser's "Search online" entry aside),
        so a real app or file match is never pushed down the list. 'default' leaves
        it where GNOME Shell puts it.

        This moves the section, not the keyboard selection: GNOME picks what Enter
        activates by provider registration order, so with another section showing,
        Enter still goes to that one.
      </description>
    </key>

  </schema>
</schemalist>
