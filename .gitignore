node_modules/
dist/
native/build/
*.shell-extension.zip

# Agent scratch space; debug logs here can contain queries the user typed.
.cursor/

# Built packages and the assembled Pages site: regenerated by `make deb rpm repos`.
build/

# The repository signing key's private half. Never commit this.
.repo-signing-private.asc
